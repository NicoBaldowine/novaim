import { Wordmark } from "./brand-mark";
export function LogoIntro() {
  return <section className="logo-cover" id="top" aria-label="NovaIM">
    <h1><Wordmark /></h1>
    <p className="logo-rationale">Two forms join to create an N: independent capabilities, one shared direction. Its forward lean suggests momentum; its soft curves keep the identity open and approachable. Layers and flowing lines extend this idea across the visual system.</p>
  </section>;
}
